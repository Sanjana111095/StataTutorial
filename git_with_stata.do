! git init .
! git add stata_tut.stpr
! git commit -m "learning to use git on stata"
! git add stata_tut.do
! git commit -m "adding do file"
! git remote add origin https://github.com/Sanjana111095/StataTutorial.git
! git push origin master
! git add git_with_stata.do
! git commit -m "adding git command do file"
! git push origin master
